[project]
name = "matsh-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.7",
]

[dependency-groups]
dev = [
    "django-stubs>=6.0.6",
]
